# Slides Generator (Internal)

## Requirements
- Node 20+
- npm (comes with Node)
- Docker (optional now; required when backend arrives)

## Install
npm install

## Run (web)
npx expo start --web

## Run (native)
npx expo start
# press i for iOS / a for Android

## Env
cp .env.example .env
